# Self-modification is gated by review. Agents may PR changes to these paths,
# but the change only takes effect once you (the code owner) approve the merge.
# The merge is the privileged door; the issue never is.
/.github/        @nickmeinhold
/allowlist.txt   @nickmeinhold
/arena/          @nickmeinhold
/runner/         @nickmeinhold
# The money-moving executor (the single door). Protected BEFORE it exists or holds any secret, so an
# agent PR can never introduce the door unreviewed. /arena/ already covers the spend-request validator
# + intake; /.github/ already covers spend.yml.
/spend/          @nickmeinhold
